name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ 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 }}
          GIST_ID: ${{ e98adddf28d7c3b12ec93c342460a40c#file-productive-box }}
          TIMEZONE: Asia/Seoul
