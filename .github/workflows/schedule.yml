name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.22952a9c854361a1f2024f69b1bc39cd }}
          TIMEZONE: Asia/Seoul
