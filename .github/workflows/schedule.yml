name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ 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 }}
          GIST_ID: ${{ https://gist.github.com/SJPark97/ }}
          TIMEZONE: Asia/Seoul
